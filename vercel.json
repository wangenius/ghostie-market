{
	"ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
	"builds": [
		{
			"src": "src/**/*.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/plugins/(.*)",
			"dest": "src/index.ts"
		},
		{
			"src": "/(.*)",
			"dest": "src/index.ts"
		}
	]
}