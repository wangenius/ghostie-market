{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/plugins/(.*)",
			"dest": "/plugins/$1"
		},
		{
			"src": "/(.*)",
			"dest": "src/index.ts"
		}
	],
	"ignoreCommand": "git diff --quiet HEAD^ HEAD ./plugins/"
}